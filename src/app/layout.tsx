import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "DailyEssentials - Your Daily Life Products Store",
  description:
    "Shop quality daily essentials online. From home care to personal care, find everything you need.",
  icons: {
    icon: "/logo.png", // your single favicon
    apple: "/logo.png",
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
